<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cart - FreshMarket</title>
    <style>
        /* Base styles matching shopping.ejs */
        * { margin:0; padding:0; box-sizing:border-box; }
        body {
            background: linear-gradient(135deg,#f5f7ff 0%,#eef6fb 100%);
            min-height: 100vh;
            font-family: 'Segoe UI', Roboto, Arial, sans-serif;
        }
        
        /* Nav styles matching shopping.ejs */
        .nav { background:rgba(255,255,255,.95); padding:1rem 0; box-shadow:0 2px 10px rgba(0,0,0,.08); }
        .container { width:90%; max-width:1200px; margin:0 auto; }
        .nav-content { display:flex; justify-content:space-between; align-items:center; }
        .logo { font-size:1.4rem; font-weight:800; color:#667eea; }
        .nav-links { display:flex; gap:1rem; }
        .nav-links a { color:#444; text-decoration:none; padding:.5rem 1rem; border-radius:.5rem; transition:all .2s; }
        .nav-links a:hover { background:#667eea; color:#fff; }

        /* Cart specific styles */
        .cart-container {
            display: grid;
            grid-template-columns: 1fr 320px;
            gap: 2rem;
            padding: 2rem 0;
        }
        
        .cart-items {
            background: #fff;
            border-radius: 1rem;
            padding: 1.5rem;
            box-shadow: 0 4px 12px rgba(0,0,0,.05);
        }
        
        .cart-item {
            display: grid;
            grid-template-columns: 100px 1fr auto auto;
            gap: 1rem;
            align-items: center;
            padding: 1rem;
            border-bottom: 1px solid #eee;
        }
        
        .cart-item:last-child { border:none; }
        
        .item-img {
            width: 100%;
            aspect-ratio: 1;
            object-fit: cover;
            border-radius: .5rem;
        }
        
        .item-info h3 {
            font-size: 1.1rem;
            margin-bottom: .25rem;
        }
        
        .item-price {
            color: #667eea;
            font-weight: 600;
        }
        
        .quantity-controls {
            display: flex;
            align-items: center;
            gap: .5rem;
        }
        
        .qty-btn {
            padding: .5rem;
            width: 32px;
            height: 32px;
            border: none;
            border-radius: .5rem;
            background: #f1f5f9;
            cursor: pointer;
            transition: all .2s;
        }
        
        .qty-btn:hover { background: #e2e8f0; }
        
        .cart-summary {
            background: #fff;
            border-radius: 1rem;
            padding: 1.5rem;
            position: sticky;
            top: 2rem;
            box-shadow: 0 4px 12px rgba(0,0,0,.05);
        }
        
        .summary-row {
            display: flex;
            justify-content: space-between;
            padding: .75rem 0;
            border-bottom: 1px solid #eee;
        }
        
        .summary-row:last-child {
            border: none;
            font-size: 1.2rem;
            font-weight: 600;
            color: #667eea;
        }
        
        .checkout-btn {
            display: block;
            width: 100%;
            padding: 1rem;
            margin-top: 1rem;
            border: none;
            border-radius: .5rem;
            background: linear-gradient(135deg,#667eea,#764ba2);
            color: #fff;
            font-weight: 600;
            cursor: pointer;
            transition: all .2s;
        }
        
        .checkout-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(102,126,234,.3);
        }
        
        .empty-cart {
            text-align: center;
            padding: 3rem 1rem;
            background: #fff;
            border-radius: 1rem;
            grid-column: 1 / -1;
        }
        
        @media (max-width: 768px) {
            .cart-container {
                grid-template-columns: 1fr;
            }
            .cart-item {
                grid-template-columns: 80px 1fr;
                gap: .75rem;
            }
            .item-total {
                grid-column: 1 / -1;
                text-align: right;
            }
        }
    </style>
</head>
<body>
    <nav class="nav">
        <div class="container">
            <div class="nav-content">
                <div class="logo">🛒 FreshMarket</div>
                <div class="nav-links">
                    <a href="/shopping">Shop</a>
                    <a href="/cart">Cart</a>
                    <a href="/logout">Logout</a>
                </div>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="cart-container">
            <% if (!cart || cart.length === 0) { %>
                <div class="empty-cart">
                    <h2>Your cart is empty</h2>
                    <p style="color:#666;margin:1rem 0">Browse our products and add some items to your cart</p>
                    <a href="/shopping" class="checkout-btn" style="display:inline-block;width:auto;padding:.75rem 2rem">Continue Shopping</a>
                </div>
            <% } else { %>
                <div class="cart-items">
                    <h2 style="margin-bottom:1.5rem">Shopping Cart (<%= cart.length %> items)</h2>
                    <% let subtotal = 0 %>
                    <% cart.forEach(function(item) { %>
                        <% subtotal += item.price * item.quantity %>
                        <div class="cart-item">
                            <img src="/images/<%= item.image %>" alt="<%= item.productName %>" class="item-img">
                            <div class="item-info">
                                <h3><%= item.productName %></h3>
                                <p class="item-price">$<%= item.price.toFixed(2) %></p>
                            </div>
                            <div class="quantity-controls">
                                <button class="qty-btn">-</button>
                                <span><%= item.quantity %></span>
                                <button class="qty-btn">+</button>
                            </div>
                            <div class="item-total">
                                $<%= (item.price * item.quantity).toFixed(2) %>
                            </div>
                        </div>
                    <% }); %>
                </div>

                <div class="cart-summary">
                    <h3 style="margin-bottom:1rem">Order Summary</h3>
                    <div class="summary-row">
                        <span>Subtotal</span>
                        <span>$<%= subtotal.toFixed(2) %></span>
                    </div>
                    <div class="summary-row">
                        <span>Shipping</span>
                        <span>Free</span>
                    </div>
                    <div class="summary-row">
                        <span>Total</span>
                        <span>$<%= subtotal.toFixed(2) %></span>
                    </div>
                    <button class="checkout-btn">Proceed to Checkout</button>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>
