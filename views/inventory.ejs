<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inventory Management - FreshMarket</title>
    <style>
        * { margin:0; padding:0; box-sizing:border-box; }
        body {
            background: linear-gradient(135deg,#f5f7ff 0%,#eef6fb 100%);
            min-height: 100vh;
            font-family: 'Segoe UI', Roboto, Arial, sans-serif;
        }
        
        .nav { background:rgba(255,255,255,.95); padding:1rem 0; box-shadow:0 2px 10px rgba(0,0,0,.08); }
        .container { width:90%; max-width:1200px; margin:0 auto; }
        .nav-content { display:flex; justify-content:space-between; align-items:center; }
        .logo { font-size:1.4rem; font-weight:800; color:#667eea; }
        .nav-links { display:flex; gap:1rem; }
        .nav-links a { color:#444; text-decoration:none; padding:.5rem 1rem; border-radius:.5rem; transition:all .2s; }
        .nav-links a:hover { background:#667eea; color:#fff; }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin: 2rem 0;
        }

        .add-product-btn {
            background: linear-gradient(135deg,#667eea,#764ba2);
            color: #fff;
            padding: .8rem 1.5rem;
            border-radius: .5rem;
            text-decoration: none;
            font-weight: 600;
            transition: all .2s;
        }
        .add-product-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(102,126,234,.3);
        }

        .controls {
            display: flex;
            gap: 1rem;
            margin: 1rem 0;
            padding: 1rem;
            background: #fff;
            border-radius: .8rem;
            box-shadow: 0 4px 12px rgba(0,0,0,.05);
        }
        .search-input {
            flex: 1;
            padding: .8rem 1rem;
            border: 2px solid #eee;
            border-radius: .5rem;
            font-size: .95rem;
        }
        
        .products-table {
            background: #fff;
            border-radius: 1rem;
            padding: 1rem;
            box-shadow: 0 4px 12px rgba(0,0,0,.05);
            width: 100%;
            border-collapse: collapse;
        }

        .products-table th {
            text-align: left;
            padding: 1rem;
            border-bottom: 2px solid #eee;
            color: #444;
            font-weight: 600;
        }

        .products-table td {
            padding: 1rem;
            border-bottom: 1px solid #eee;
            vertical-align: middle;
        }

        .product-img {
            width: 60px;
            height: 60px;
            object-fit: cover;
            border-radius: .5rem;
        }

        .actions {
            display: flex;
            gap: .5rem;
        }

        .btn {
            padding: .5rem 1rem;
            border: none;
            border-radius: .5rem;
            cursor: pointer;
            font-weight: 500;
            transition: all .2s;
        }

        .btn-edit {
            background: #eef2ff;
            color: #667eea;
        }
        .btn-delete {
            background: #fef2f2;
            color: #ef4444;
        }
        .btn:hover {
            transform: translateY(-2px);
        }

        .status {
            padding: .25rem .75rem;
            border-radius: 1rem;
            font-size: .875rem;
            font-weight: 500;
        }
        .in-stock { background: #dcfce7; color: #166534; }
        .low-stock { background: #fef3c7; color: #92400e; }
        .out-of-stock { background: #fef2f2; color: #991b1b; }
    </style>
</head>
<body>
    <nav class="nav">
        <div class="container">
            <div class="nav-content">
                <div class="logo">🛒 FreshMarket</div>
                <div class="nav-links">
                    <a href="/inventory">Inventory</a>
                    <a href="/addProduct">Add Product</a>
                    <a href="/logout">Logout</a>
                </div>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="header">
            <h1>Inventory Management</h1>
            <a href="/addProduct" class="add-product-btn">+ Add Product</a>
        </div>

        <div class="controls">
            <input type="text" id="searchInput" class="search-input" placeholder="Search products...">
        </div>

        <table class="products-table">
            <thead>
                <tr>
                    <th>Image</th>
                    <th>Product Name</th>
                    <th>Price</th>
                    <th>Stock</th>
                    <th>Status</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody id="productsBody">
                <% products.forEach(function(product) { %>
                    <tr data-name="<%= product.productName.toLowerCase() %>">
                        <td><img src="/images/<%= product.image %>" class="product-img" alt="<%= product.productName %>"></td>
                        <td><%= product.productName %></td>
                        <td>$<%= product.price.toFixed(2) %></td>
                        <td><%= product.quantity %></td>
                        <td>
                            <% if (product.quantity > 10) { %>
                                <span class="status in-stock">In Stock</span>
                            <% } else if (product.quantity > 0) { %>
                                <span class="status low-stock">Low Stock</span>
                            <% } else { %>
                                <span class="status out-of-stock">Out of Stock</span>
                            <% } %>
                        </td>
                        <td class="actions">
                            <a href="/updateProduct/<%= product.id %>" class="btn btn-edit">Edit</a>
                            <button onclick="deleteProduct(<%= product.id %>)" class="btn btn-delete">Delete</button>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>

    <script>
        // Search functionality
        const searchInput = document.getElementById('searchInput');
        const productsBody = document.getElementById('productsBody');
        const rows = Array.from(productsBody.getElementsByTagName('tr'));

        searchInput.addEventListener('input', function() {
            const searchTerm = this.value.toLowerCase();
            
            rows.forEach(row => {
                const name = row.dataset.name;
                row.style.display = name.includes(searchTerm) ? '' : 'none';
            });
        });

        // Delete confirmation
        function deleteProduct(id) {
            if (confirm('Are you sure you want to delete this product?')) {
                window.location.href = `/deleteProduct/${id}`;
            }
        }
    </script>
</body>
</html>
