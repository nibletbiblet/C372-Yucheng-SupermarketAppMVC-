<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login / Register - FreshMarket</title>
    <style>
        * { margin:0; padding:0; box-sizing:border-box; }
        body {
            background: url('/images/backgrounds/login.jpg') no-repeat center center fixed;
            background-size: cover;
            font-family: 'Segoe UI', sans-serif;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }
        body::before {
            content: '';
            position: absolute;
            top: 0; left: 0;
            width: 100%; height: 100%;
            background: rgba(102, 126, 234, 0.88);
            z-index: 0;
        }
        
        .container {
            position: relative;
            z-index: 1;
            width: 95%;
            max-width: 1100px;
            height: 650px;
            overflow: hidden;
            border-radius: 2rem;
            box-shadow: 0 25px 80px rgba(0,0,0,0.4);
        }
        
        .forms-wrapper {
            display: flex;
            width: 200%;
            height: 100%;
            transition: transform 0.6s cubic-bezier(0.68, -0.55, 0.265, 1.55);
        }
        
        .forms-wrapper.show-register {
            transform: translateX(-50%);
        }
        
        .form-section {
            width: 50%;
            background: rgba(255,255,255,0.98);
            padding: 4rem;
            display: flex;
            flex-direction: column;
            justify-content: center;
            overflow-y: auto;
        }
        
        .logo { text-align: center; margin-bottom: 2rem; }
        .logo h1 { font-size: 3rem; color: #667eea; }
        
        .form-title { text-align: center; margin-bottom: 2rem; color: #333; font-size: 2.2rem; }
        
        .form-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem; }
        .form-group { margin-bottom: 1.5rem; }
        .form-group.full-width { grid-column: 1 / -1; }
        .form-group label { display: block; margin-bottom: .7rem; font-weight: 600; color: #555; font-size: 1rem; }
        .form-group input, .form-group select {
            width: 100%;
            padding: 1rem 1.2rem;
            border: 2px solid #e2e8f0;
            border-radius: .7rem;
            font-size: 1rem;
            transition: all .3s;
        }
        .form-group input:focus, .form-group select:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 4px rgba(102,126,234,0.1);
        }
        
        .password-wrapper { position: relative; }
        .toggle-password {
            position: absolute;
            right: 12px;
            top: 50%;
            transform: translateY(-50%);
            background: none;
            border: none;
            cursor: pointer;
            font-size: 1.4rem;
        }
        
        .btn-submit {
            width: 100%;
            padding: 1.2rem;
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: #fff;
            border: none;
            border-radius: .8rem;
            font-weight: 700;
            font-size: 1.2rem;
            cursor: pointer;
            transition: all .3s;
            margin-top: 1.5rem;
        }
        .btn-submit:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(102,126,234,.5);
        }
        
        .switch-form {
            text-align: center;
            margin-top: 1.5rem;
            color: #666;
            font-size: 1rem;
        }
        .switch-btn {
            background: none;
            border: none;
            color: #667eea;
            font-weight: 700;
            cursor: pointer;
            text-decoration: underline;
            font-size: 1rem;
        }
        
        .alert {
            padding: 1rem;
            border-radius: .7rem;
            margin-bottom: 1.5rem;
            text-align: center;
            font-size: 1rem;
        }
        .alert-error { background: #fef2f2; color: #991b1b; border-left: 4px solid #ef4444; }
        .alert-success { background: #dcfce7; color: #065f46; border-left: 4px solid #10b981; }
        
        @media (max-width: 768px) {
            .container { height: auto; max-height: 95vh; width: 98%; }
            .form-section { padding: 2.5rem; }
            .form-grid { grid-template-columns: 1fr; gap: 1rem; }
            .logo h1 { font-size: 2.2rem; }
            .form-title { font-size: 1.8rem; }
            .form-group { margin-bottom: 1rem; }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="forms-wrapper" id="formsWrapper">
            <!-- Login Form -->
            <div class="form-section">
                <div class="logo">
                    <h1>🛒 FreshMarket</h1>
                </div>
                <h2 class="form-title">Welcome Back!</h2>
                
                <% if (errors && errors.length > 0) { %>
                    <div class="alert alert-error"><%= errors[0] %></div>
                <% } %>
                <% if (messages && messages.length > 0) { %>
                    <div class="alert alert-success"><%= messages[0] %></div>
                <% } %>
                
                <form action="/login" method="POST">
                    <div class="form-group">
                        <label>Email Address</label>
                        <input type="email" name="email" required placeholder="your@email.com" autocomplete="off">
                    </div>
                    
                    <div class="form-group">
                        <label>Password</label>
                        <div class="password-wrapper">
                            <input type="password" name="password" id="loginPassword" required placeholder="Enter your password" autocomplete="new-password">
                            <button type="button" class="toggle-password" onclick="togglePassword('loginPassword', this)">👁️</button>
                        </div>
                    </div>
                    
                    <button type="submit" class="btn-submit">Login</button>
                </form>
                
                <div class="switch-form">
                    Don't have an account? <button type="button" class="switch-btn" onclick="showRegister()">Register here</button>
                </div>
            </div>
            
            <!-- Register Form -->
            <div class="form-section">
                <div class="logo">
                    <h1>🛒 FreshMarket</h1>
                </div>
                <h2 class="form-title">Create Account</h2>
                
                <% if (registerErrors && registerErrors.length > 0) { %>
                    <div class="alert alert-error"><%= registerErrors[0] %></div>
                <% } %>
                
                <form action="/register" method="POST">
                    <div class="form-grid">
                        <div class="form-group">
                            <label>Full Name</label>
                            <input type="text" name="username" required placeholder="John Doe" value="<%= formData ? formData.username : '' %>">
                        </div>
                        
                        <div class="form-group">
                            <label>Email Address</label>
                            <input type="email" name="email" required placeholder="your@email.com" value="<%= formData ? formData.email : '' %>">
                        </div>
                        
                        <div class="form-group">
                            <label>Password</label>
                            <div class="password-wrapper">
                                <input type="password" name="password" id="regPassword" required placeholder="Min 8 characters" autocomplete="new-password">
                                <button type="button" class="toggle-password" onclick="togglePassword('regPassword', this)">👁️</button>
                            </div>
                        </div>
                        
                        <div class="form-group">
                            <label>Confirm Password</label>
                            <div class="password-wrapper">
                                <input type="password" name="confirmPassword" id="confirmPassword" required placeholder="Re-enter password" autocomplete="new-password">
                                <button type="button" class="toggle-password" onclick="togglePassword('confirmPassword', this)">👁️</button>
                            </div>
                        </div>
                        
                        <div class="form-group full-width">
                            <label>Address</label>
                            <input type="text" name="address" required placeholder="123 Street Name" value="<%= formData ? formData.address : '' %>">
                        </div>
                        
                        <div class="form-group">
                            <label>Contact Number</label>
                            <input type="tel" name="contact" required placeholder="+65 1234 5678" value="<%= formData ? formData.contact : '' %>">
                        </div>
                        
                        <div class="form-group">
                            <label>Account Type</label>
                            <select name="role" required>
                                <option value="">Select Role</option>
                                <option value="user">Customer</option>
                                <option value="admin">Admin</option>
                            </select>
                        </div>
                    </div>
                    
                    <button type="submit" class="btn-submit">Create Account</button>
                </form>
                
                <div class="switch-form">
                    Already have an account? <button type="button" class="switch-btn" onclick="showLogin()">Login here</button>
                </div>
            </div>
        </div>
    </div>
    
    <script>
        function showRegister() {
            document.getElementById('formsWrapper').classList.add('show-register');
        }
        
        function showLogin() {
            document.getElementById('formsWrapper').classList.remove('show-register');
        }
        
        function togglePassword(inputId, button) {
            const input = document.getElementById(inputId);
            if (input.type === 'password') {
                input.type = 'text';
                button.textContent = '🙈';
            } else {
                input.type = 'password';
                button.textContent = '👁️';
            }
        }
        
        // Auto-slide to register if there are register errors
        <% if (registerErrors && registerErrors.length > 0) { %>
            showRegister();
        <% } %>
    </script>
</body>
</html>
